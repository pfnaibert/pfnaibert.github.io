
Or using the `dplyr` environment.

```{r}
tmp <- gdp.df %>%
filter(dates > "2019-01-01" ) %>%
select(nominal, ret1, ret4, ret.ac4q.real)
kable( t( apply( tmp, 2, rev) ), digits=2, caption = "GDP by Quarter")
```

```{r, error = T}
tmp <- gdp.df %>%
filter(dates > "2019-01-01" ) %>%
select(nominal, ret1, ret4, ret.ac4q.real) %>%
mutate(nominal = nominal / 1000)

kable( t( apply( tmp, 2, rev) ), digits=2, caption = "GDP by Quarter")
```

And it LOSES the date attribution? WHY?
Really, why? leave me a comment or create an issue in the repo.

```{r, error = T}
tmp <- gdp.df %>%
filter(dates > "2019-01-01" ) %>%
select(nominal, ret1, ret4, ret.ac4q.real)
tmp[,1] <- tmp[,1]/100
kable( t( apply( tmp, 2, rev) ), digits=2, caption = "GDP by Quarter")
```
